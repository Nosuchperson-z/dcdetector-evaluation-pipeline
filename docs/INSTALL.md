# Install Notes

1. Clone this minimal public release.
2. Place the official DCdetector repository at `KDD2023-DCdetector/`.
3. Prepare a Python environment with the dependencies required by the scripts you want to run.
4. Prepare local `data_raw/`, `data_processed/`, and `outputs/` directories.

This release is intended as a compact code release, not a turnkey reproduction package.
